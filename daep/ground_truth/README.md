# Ground Truth

This folder stores fixed ground-truth data shared by the ROS and offline
experiment reports.

- `world_tree_ground_truth.csv`: canonical `world_jean` tree positions. These
  are the mesh-offset-corrected trunk centers previously generated from
  `world_jean.world`.
